# Subset of requirements.txt actually vendored into the Catalyst AppSail
# deploy bundle (./vendor_deps, added to PYTHONPATH by app-config.json's
# command). Deliberately excludes chromadb/sentence-transformers — those pull
# in torch (400MB+), too large to hand-vendor for a managed-runtime deploy
# whose platform doesn't run pip install server-side. services/vector_memory_service.py
# degrades to a no-op when these two aren't importable; every other feature is
# unaffected. Local dev still installs the full requirements.txt as usual.
fastapi>=0.138
uvicorn>=0.49
requests>=2.34
python-dotenv>=1.2
pydantic>=2.13
PyJWT>=2.10
bcrypt>=4.0
fpdf2>=2.8
python-multipart>=0.0.32
